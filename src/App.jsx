import React, { useState, useEffect } from 'react';
import { motion, AnimatePresence } from 'framer-motion';
import { Calendar, RefreshCw, ExternalLink, Award, FileText } from 'lucide-react';
import './index.css';

function App() {
  const [activeCategory, setActiveCategory] = useState('All');
  const [selectedProduct, setSelectedProduct] = useState(null);
  const [feedData, setFeedData] = useState({ last_updated: null, item_count: 0, data: [] });
  const [isLoading, setIsLoading] = useState(true);

  const fetchFeed = async () => {
    setIsLoading(true);
    try {
      // In MVP, we fetch the static feed.json generated by our daily batch
      const timestamp = new Date().getTime(); // cache busting
      const res = await fetch(`/feed.json?t=${timestamp}`);
      if (res.ok) {
        const jsonData = await res.json();
        setFeedData(jsonData);
      } else {
        console.error("Failed to fetch feed");
      }
    } catch (e) {
      console.error("Feed error:", e);
    } finally {
      setIsLoading(false);
    }
  };

  useEffect(() => {
    fetchFeed();
  }, []);

  // Extract unique categories
  const categories = ['All', ...new Set(feedData.data.map(item => item.category))].filter(Boolean);

  const filteredProducts = activeCategory === 'All' 
    ? feedData.data 
    : feedData.data.filter(p => p.category === activeCategory);

  const topTrending = feedData.data.slice(0, 6); // Just highlight the top 6

  const openModal = (product) => {
    setSelectedProduct(product);
    document.body.style.overflow = 'hidden';
  };

  const closeModal = () => {
    setSelectedProduct(null);
    document.body.style.overflow = 'auto';
  };

  const formatDate = (isoString) => {
    if (!isoString) return '';
    const d = new Date(isoString);
    return `${d.getMonth()+1}/${d.getDate()} ${d.getHours()}:${d.getMinutes().toString().padStart(2, '0')}`;
  };

  return (
    <div className="container">
      <header className="header">
        <motion.h1 
          initial={{ y: -10, opacity: 0 }} 
          animate={{ y: 0, opacity: 1 }}
        >
          Trending Crypto Product Radar
        </motion.h1>
        <motion.p 
          className="briefing"
          initial={{ y: -10, opacity: 0 }} 
          animate={{ y: 0, opacity: 1 }} 
          transition={{ delay: 0.1 }}
        >
          Discover new crypto products before they become obvious. Today’s notable launches, funding moves, and major updates curated into a clean digest.
        </motion.p>
        
        <div className="meta-info">
          <Calendar size={14} /> 
          <span>Last Updated: {feedData.last_updated ? new Date(feedData.last_updated).toLocaleString('ko-KR') : 'Loading...'}</span>
          <button 
            onClick={fetchFeed} 
            disabled={isLoading}
            style={{ marginLeft: 'auto', background: 'none', border: '1px solid var(--border-color)', color: 'var(--text-main)', padding: '6px 12px', borderRadius: '4px', cursor: 'pointer', display: 'flex', alignItems: 'center', gap: '6px', fontSize: '0.8rem' }}
          >
            <RefreshCw size={12} className={isLoading ? 'spin' : ''} />
            새로고침
          </button>
        </div>
      </header>

      {isLoading ? (
        <div style={{ textAlign: 'center', padding: '4rem', color: 'var(--text-dim)' }}>
          데이터를 불러오거나 AI 분석을 진행하는 중입니다...
        </div>
      ) : feedData.data.length === 0 ? (
        <div style={{ textAlign: 'center', padding: '4rem', color: 'var(--text-dim)' }}>
          No notable new products detected today. Check back after the next batch update.
        </div>
      ) : (
        <>
          {feedData.daily_briefing && (
            <div className="daily-briefing-card">
              <h2>📰 오늘의 큐레이션 (Daily Briefing)</h2>
              <div className="briefing-content">
                {feedData.daily_briefing.split('\n').map((line, idx) => (
                  <p key={idx}>{line}</p>
                ))}
              </div>
            </div>
          )}

          <h2 className="section-title">🔥 Top Trending ({topTrending.length})</h2>
          <motion.div layout className="grid">
            <AnimatePresence>
              {topTrending.map((product) => (
                <motion.div
                  key={product.id}
                  layout
                  initial={{ opacity: 0, y: 20 }}
                  animate={{ opacity: 1, y: 0 }}
                  transition={{ duration: 0.3 }}
                  className="card"
                  onClick={() => openModal(product)}
                >
                  <div className="card-header">
                    <span className="event-badge">{product.event_type}</span>
                    <span className="category-tag">{product.category}</span>
                  </div>
                  <h2>{product.name}</h2>
                  <p className="card-summary">{product.one_line_summary}</p>
                  <div className="card-why">
                    <strong>💡 Why it matters:</strong> {product.why_it_matters}
                  </div>
                  <div className="card-footer">
                    <span>Score: {product.trending_score}</span>
                    <span>{product.source_count} Sources</span>
                  </div>
                </motion.div>
              ))}
            </AnimatePresence>
          </motion.div>

          <h2 className="section-title" style={{ marginTop: '5rem' }}>🎯 All Radar Products</h2>
          <div className="categories">
            {categories.map(cat => (
              <button 
                key={cat}
                className={`category-btn ${activeCategory === cat ? 'active' : ''}`}
                onClick={() => setActiveCategory(cat)}
              >
                {cat}
              </button>
            ))}
          </div>

          <motion.div layout className="grid">
            <AnimatePresence>
              {filteredProducts.map((product) => (
                <motion.div
                  key={product.id + '_all'}
                  layout
                  initial={{ opacity: 0 }}
                  animate={{ opacity: 1 }}
                  exit={{ opacity: 0, scale: 0.9 }}
                  transition={{ duration: 0.2 }}
                  className="card"
                  onClick={() => openModal(product)}
                >
                  <div className="card-header">
                    <span className="event-badge">{product.event_type}</span>
                  </div>
                  <h2>{product.name}</h2>
                  <p className="card-summary">{product.one_line_summary}</p>
                  <div className="card-footer" style={{ borderTop: 'none', paddingTop: 0, marginTop: 'auto' }}>
                    <span>{product.category}</span>
                    <span>{formatDate(product.detected_date)}</span>
                  </div>
                </motion.div>
              ))}
            </AnimatePresence>
          </motion.div>
        </>
      )}

      {/* Detail Modal */}
      <AnimatePresence>
        {selectedProduct && (
          <motion.div 
            className="modal-overlay"
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            exit={{ opacity: 0 }}
            onClick={closeModal}
          >
            <motion.div 
              className="modal-content"
              initial={{ y: 30, opacity: 0 }}
              animate={{ y: 0, opacity: 1 }}
              exit={{ y: 30, opacity: 0 }}
              onClick={(e) => e.stopPropagation()}
            >
              <button className="close-btn" onClick={closeModal}>✕</button>
              
              <div className="modal-header">
                <span className="event-badge">{selectedProduct.event_type}</span>
                <span className="category-tag" style={{ marginLeft: '1rem' }}>{selectedProduct.category}</span>
                <h1>{selectedProduct.name}</h1>
              </div>

              <p className="modal-summary">
                {selectedProduct.one_line_summary}
              </p>

              <div className="modal-why">
                <strong>Why it matters:</strong>
                <br />
                {selectedProduct.why_it_matters}
              </div>

              <div className="modal-links">
                <h3><FileText size={18} style={{ display: 'inline', verticalAlign: 'middle', marginRight: '6px' }} /> Related Articles & Links</h3>
                <ul>
                  {selectedProduct.official_link && (
                    <li>
                      <a href={selectedProduct.official_link} target="_blank" rel="noreferrer">
                        <Award size={16} /> Official Website / Announcement
                      </a>
                    </li>
                  )}
                  {selectedProduct.source_links.map((link, idx) => (
                    <li key={idx}>
                      <a href={link.url} target="_blank" rel="noreferrer">
                        <ExternalLink size={16} /> {link.name} (Source)
                      </a>
                    </li>
                  ))}
                </ul>
              </div>

            </motion.div>
          </motion.div>
        )}
      </AnimatePresence>
    </div>
  );
}

export default App;
